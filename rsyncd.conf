lock file = /var/run/rsync.lock
log file = /var/log/rsyncd.log
pid file = /var/run/rsyncd.pid

[hackage]
    path = /hackage
    comment = Public read-only Hackage archive
    uid = root
    gid = root
    read only = yes
    list = yes
